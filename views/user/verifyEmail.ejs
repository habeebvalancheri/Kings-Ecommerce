<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Kings</title>
  <link rel="icon" type="image/x-icon" href="images/fav.png">
  <!-- Font Awesome -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet" />
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap" rel="stylesheet" />
  <!-- Bootstrap CSS -->
  <link rel='stylesheet' href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css'>
  <!-- MDB CSS -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.4.2/mdb.min.css" rel="stylesheet" />
  <style>
    .otp-field {
      display: flex;
      justify-content: space-between;
    }

    .otp-input {
      width: 40px;
      height: 40px;
      margin: 0 5px;
      text-align: center;
      font-size: 1.2em;
      border: 1px solid #ccc;
      border-radius: 5px;
      outline: none;
    }

    .verify-heading {
      text-align: left;
    }

    .otp-message {
      text-align: left;
      margin-bottom: 10px;
    }

    .resend {
      text-align: left;
      margin-bottom: 20px;
    }

    .text-left {
      text-align: left;
    }

    .errorP {
      color: red;
      font-size: small;
    }

    @media (max-width: 768px) {
      .card-body {
        padding: 3rem;
      }

      .btn {
        height: auto;
        padding: 1rem;
      }
    }

    @media (max-width: 576px) {
      .card-body {
        padding: 2rem;
      }

      .otp-input {
        width: 30px;
        height: 30px;
        font-size: 1em;
      }

      .btn {
        font-size: 0.9em;
        height: auto;
        padding: 0.75rem;
      }
    }
  </style>
</head>

<body>
  <section class="container-fluid bg-body-tertiary d-flex align-items-center justify-content-center" style="height: 100vh;">
    <div class="row justify-content-center">
      <div class="col-12 col-md-8 col-lg-6">
        <div class="card mb-4 border-0">
          <div class="card-body p-4 p-md-5 text-center" style="
            border-radius: 20px;
            background: rgba(220, 220, 220, 0.40);
            box-shadow: 5px 15px 15px rgba(0, 0, 0, 0.20);
          ">
            <h4 class="mb-4">Reset Password</h4>
            <p class="otp-message">
              To reset your password, enter your registered email. We'll send a One Time Password (OTP) to reset your password.
            </p>
            <form action="/sendResetPasswordOTP" method="post">
              <!-- Add Email input box -->
              <div class="mb-3">
                <input type="email" name="email" class="form-control" placeholder="Enter your registered email" required />
              </div>
              
              <p class="errorP <%= !noUserFound?'d-none':''  %>" ><%=noUserFound  %></p>
              <p class="errorP <%= !errorEmail3?'d-none':''  %>" ><%=errorEmail3  %></p>
              <p class="errorP <%= !errorPattern3?'d-none':''  %>" ><%=errorPattern3  %></p>

              <!-- Submit button -->
              <button class="btn verifyBtn mb-3" 
                style="border: 1px solid #000; background: #00DCFA; color:#000; width: 100%; height: 50px;" 
                type="submit">
                Send OTP
              </button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </section>
</body>

</html>
