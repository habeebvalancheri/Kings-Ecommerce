<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Kings</title>
  <link rel="icon" type="image/x-icon" href="images/fav-icon.png">
  <!-- Font Awesome -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet" />
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap" rel="stylesheet" />
  <!-- MDB -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.4.2/mdb.min.css" rel="stylesheet" />

  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Rubik&display=swap" rel="stylesheet">

  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css">



</head>

<style>
.card-title {
    display: -webkit-box;
    -webkit-box-orient: vertical;
    overflow: hidden;
    -webkit-line-clamp: 2;
    /* number of lines to show */
  }

  /* Responsive styles */
  @media (max-width: 768px) {
    .navbar-toggler {
      margin-top: 10px;
    }

    .navbar-nav {
      flex-direction: column;
      background-color: #232F3E;
    }

    .d-flex {
      justify-content: center;
    }

    .float-end {
      text-align: center;
    }

    .input-group {
      width: 100%;
      margin-top: 10px;
    }

    #offerBanner img {
      object-fit: cover;
      width: 100%;
      height: 200px; /* Adjust the height as needed */
    }

    .carousel-caption {
      background: rgba(0, 0, 0, 0.5);
      color: #fff;
      text-align: center;
    }

    .carousel-caption h5,
    .carousel-caption p {
      font-size: 14px;
      margin: 5px;
    }

    .container.my-5 header h3 {
      font-size: 20px;
    }

    .product-link {
      text-align: center;
    }

    .main-image,
    .hover-image {
      width: 100%;
      height: 150px; /* Adjust the height as needed */
      object-fit: cover;
    }

    .card-title,
    .card-text {
      text-align: center;
    }

    .float-center {
      text-align: center;
    }

    .input-group {
      width: 100%;
      margin-top: 10px;
      flex-direction: column;
      align-items: center;
    }

    .input-group .btn {
      width: 100%;
      margin-top: 10px;
    }

    .form-outline {
      width: 100%;
      margin-top: 10px;
    }

    #form1 {
      width: 100%;
    }
  }
  
</style>


<body style="background-color: ghostwhite">
  <!--Main Navigation-->
  <header>
    <!-- Jumbotron -->
    
    <div class="p-3 text-center " style="
    background: rgba(0, 194, 255, 0.35);">
      <div class="container">
        <div class="row gy-3">
          <!-- Left elements -->
          <div class="col-lg-2 col-sm-4 col-4">
            <a href="/" class="float-start">
              <h2 style="color: #000000; font-weight: bolder;">Kings</h2>
            </a>
          </div>
          <!-- Left elements -->

          <!-- Center elements -->
          <div class="order-lg-last col-lg-5 col-sm-8 col-8">
            <div class="d-flex float-end">
              <% if(users) {%>
                <a href="/account-details" class="me-1 py-1 px-3 nav-link" style="color: #1E3050;">
                  <i class="fas fa-user-alt m-1 me-md-2"></i>
                  <p class="d-none d-md-block mb-0">Account</p>
                </a>
                <% } else { %>
                  <a href="/signin" class="me-1  py-1 px-3 nav-link" style="color: #1E3050;"> <i
                      class="fas fa-user-alt m-1 me-md-2"></i>
                    <p class="d-none d-md-block mb-0">Login in</p>
                  </a>
                  <% } %>

                    <% if(users) {%>
                      <a href="/Wish-list" class=" py-1 px-3 nav-link" style="color: #1E3050;"> <i
                          class="fas fa-heart m-1 me-md-2 "></i>

                        <p class="d-none d-md-block mb-0">Wishlist <span id="total-wishList-items" class= "badge rounded-circle bg-danger position-absolute" >0</span></p>
                      </a>
                      <% }else{ %>
                        <a href="/signin" class=" py-1 px-3 nav-link" style="color: #1E3050;"> <i
                            class="fas fa-heart m-1 me-md-2 "></i>
                          <p class="d-none d-md-block mb-0">Wishlist</p>
                        </a>
                        <% } %>
                          <% if(users) {%>
                            <a href="/cart" class=" py-1 px-3 nav-link" 
                            style="color: #1E3050;" > <i class="fas fa-shopping-cart m-1 me-md-2 " ></i>
                            
                            <p class="d-none d-md-block mb-0">Cart <span id="total-cart-items" class= "badge rounded-circle bg-danger position-absolute" >0</span></p> 
                          </a>
                            <% }else{ %>
                              <a href="/signin" class=" py-1 px-3 nav-link" style="color: #1E3050;"> <i
                                  class="fas fa-shopping-cart m-1 me-md-2 "></i>
                                <p class="d-none d-md-block mb-0">cart</p>
                              </a>
                              <% } %>
            </div>
          </div>
          <!-- Center elements -->

          <!-- Right elements -->
          <div class="col-lg-5 col-md-12 col-12">
            <div class="input-group float-center" >

              <form action="/products" method="GET" class="input-group float-center">
                <button type="submit" class="btn  shadow-0" style="border: 1px solid #000;background: #000;">
                  <i class="fas fa-search" style="color:#fff"></i>
                </button>
                <div class="form-outline" style="width:350px;height:35px;">
                  <input type="search" name="search" id="form1" class="form-control" style="border-radius: 0 5px 5px 0;
                  border: 1px solid #000;
                  background: #FFF;width: 350px;
                  height: 35px;" />
                  <label class="form-label" for="form1">Search</label>
                </div>

              </form>

            </div>
          </div>
          <!-- Right elements -->
        </div>
      </div>
    </div>
    <!-- Jumbotron -->

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light" style="background: rgba(35, 47, 62, 0.80);">
      <!-- Container wrapper -->
      <div class="container justify-content-center justify-content-md-between">
        <!-- Toggle button -->
        <button class="navbar-toggler py-2 text-white" type="button" data-bs-toggle="collapse"
        data-bs-target="#navbarLeftAlignExample" aria-controls="navbarLeftAlignExample" aria-expanded="false"
        aria-label="Toggle navigation">
        <i class="fas fa-bars"></i>
      </button>
      

        <!-- Collapsible wrapper -->
        <div class="collapse navbar-collapse" id="navbarLeftAlignExample">
          <!-- Left links -->
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <!-- Navbar dropdown -->

            <li class="nav-item">
              <a class="nav-link text-white" href="#" style="font-family: 'Rubik', sans-serif;    font-size: 14px;
              line-height: 18px;
              font-weight: 400;
              text-transform: uppercase;
              letter-spacing: .3px;">HOME</a>
            </li>
            <li class=" nav-item">
              <a class="nav-link text-white" href="/products" style="font-family: 'Rubik', sans-serif;    font-size: 14px;
              line-height: 18px;
              font-weight: 400;
              text-transform: uppercase;
              letter-spacing: .3px;">OUR STORE</a>
            </li>
            <!-- <li class=" nav-item">
              <a class="nav-link text-white" href="#" style="font-family: 'Rubik', sans-serif;    font-size: 14px;
              line-height: 18px;
              font-weight: 400;
              text-transform: uppercase;
              letter-spacing: .3px;">BLOGS</a>
            </li>
            <li class=" nav-item">
              <a class="nav-link text-white" href="#" style="font-family: 'Rubik', sans-serif;    font-size: 14px;
              line-height: 18px;
              font-weight: 400;
              text-transform: uppercase;
              letter-spacing: .3px;">CONTACT</a>
            </li> -->

          </ul>
          <!-- Left links -->
        </div>
      </div>
      <!-- Container wrapper -->
    </nav>
    <div id="offerBanner" class="carousel slide" data-bs-ride="carousel" data-bs-interval="5000" data-bs-wrap="true">
      <div class="carousel-inner">

        <div class="carousel-item active">
          <a href="#">
            <img src="/images/Screenshot from 2024-01-27 11-41-10.png" class="d-block img-fluid w-100" alt="Image"
              style="aspect-ratio: 12/3;">
            <div class="carousel-caption d-none d-md-block">
              <h5>Special Offer!</h5>
              <p>Get amazing discounts on our featured products.</p>
            </div>
          </a>
        </div>

        <!-- Add more carousel items as needed -->

      </div>
      <!-- Carousel controls -->
      <button class="carousel-control-prev" type="button" data-bs-target="#offerBanner" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Previous</span>
      </button>
      <button class="carousel-control-next" type="button" data-bs-target="#offerBanner" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Next</span>
      </button>
    </div>
    <section>
      <div class="container my-5">
        <header class="mb-4">
          <h3 style="color: #1c1b1b; font-family: 'Rubik', sans-serif; font-size: 26px; font-weight: 500; line-height: 35px;">
            Featured Collection
          </h3>
        </header>
    
        <div class="row">
          <% for(let i = 0; i < Math.min(products.length, 12); i++) { %>
            <% var discount=products[i].discount %>
                  <% var originalPrice=products[i].price %>
                  <% var discountedPrice=originalPrice - (originalPrice * discount / 100) %>
            <div class="col-lg-2 mt-3 col-md-3 col-sm-4 d-flex">
              <a href="/product-Details?id=<%= products[i]._id %>" class="product-link"> 
              <div id="card" class="card w-100 my-2 border" style="box-shadow: 0 0 10px #0000001a;">
    
                <div class="discount-badge position-absolute top-0 start-0 mt-2 px-1 py-0"
                  style="background-color: #febd69; color: #131921; border-radius: 90px; margin-left: 8px; font-size: 0.75rem; font-weight: bolder;">
                  <%= products[i].discount %> %OFF
                </div>
    
                <img src="/images/<%= products[i].pImages[0] %>" class="card-img-top main-image" style="aspect-ratio: 2.5 / 3" />
                <img src="/images/<%= products[i].pImages[0] %>" class="card-img-top hover-image" style="aspect-ratio: 2.5 / 3; display: none;" />
    
                <div class="card-body d-flex flex-column">
                  <p class="card-text" style="color: #bf4800;">
                    <%= products[i].category.name %>
                  </p>
    
                  <h6 class="card-title" style="color: #1c1b1b; font-family: 'Rubik', sans-serif; font-size: 15px; font-weight: 600;">
                    <%= products[i].pName %>
                  </h6>
                  <p class="card-text" style="color: #e02b40;">
                    <% if(discount > 0) {%>
                      <span class="original-price"
                        style="color: #131921; text-decoration: line-through;">₹<%= originalPrice %>
                      </span>
                      <% } %>
                        ₹<%= discountedPrice  %>
                  </p>
                </div>
    
              </div>
              </a>
            </div>
          <% } %>
        </div>
      </div>
    </section>
    
  </header>


  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>

  <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"
    integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r"
    crossorigin="anonymous"></script>
    <script>
      document.addEventListener('DOMContentLoaded', async function() {
          try {
              // Fetch total items from the server
              const response = await fetch('/getTotalItemsInCart');
              const data = await response.json();
              const totalItems = data.totalQuantity;
      
              // Update the HTML element displaying total items in the navbar
              const totalItemsElement = document.getElementById('total-cart-items');
              if (totalItemsElement) {
                  if (totalItems > 0) {
                      totalItemsElement.textContent = totalItems;
                      totalItemsElement.style.display = 'inline-block'; // Show the span tag
                  } else {
                      totalItemsElement.style.display = 'none'; // Hide the span tag if totalItems is 0
                  }
              }
          } catch (error) {
              console.error('Error fetching total items in cart:', error);
          }
      });
      </script>
      <script>
        document.addEventListener('DOMContentLoaded', async function() {
            try {
                // Fetch total items from the server
                const response = await fetch('/getTotalItemsInWishList');
                const data = await response.json();
                const totalItems = data.totalQuantity;
        
                // Update the HTML element displaying total items in the navbar
                const totalItemsElement = document.getElementById('total-wishList-items');
                if (totalItemsElement) {
                    if (totalItems > 0) {
                        totalItemsElement.textContent = totalItems;
                        totalItemsElement.style.display = 'inline-block'; // Show the span tag
                    } else {
                        totalItemsElement.style.display = 'none'; // Hide the span tag if totalItems is 0
                    }
                }
            } catch (error) {
                console.error('Error fetching total items in wishList:', error);
            }
        });
        </script>
  
  <%- include('../include/footer') %>
</body>

</html>